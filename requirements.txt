# Full requirements for local dev and CI.
# Serving deps + training deps. The serving stack alone is in requirements-serve.txt.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
prometheus-client==0.21.0
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
asgi-lifespan==2.1.0

# Model + training
torch==2.4.1
transformers==4.45.2
datasets==3.0.1
scikit-learn==1.5.2
numpy==1.26.4
